INSTRUCTIONS:
1) open terminal window in directory of python files
2) enter the command “python server.py”
3) open a second terminal window in the directory of the python files
4) enter the command “python client.py”

COMMANDS:
- To change your nickname type in the command “\nick [nickname]”