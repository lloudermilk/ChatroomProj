To use: 
compile server first: "g++ server.cpp -o server"
run with one argument, a port: "./server 4000"
open new terminal window
compile client: "g++ client.cpp -o client"
run with two arguments, hostname and port from server: "./client localhost 4000"
yay!
